<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="web.spring.placecloud.persistence.LikeListMapper">
	
	<resultMap type="web.spring.placecloud.domain.LikeListVO" id="likeResultMap">
		<id property="likeId" column="LIKE_ID" />
		<result property="userEmail" column="USER_EMAIL"/>
		<result property="placeId" column="PLACE_ID" />
		<result property="placeName" column="PLACE_NAME" />
	</resultMap>
	
	<insert id="insert">
		INSERT INTO LIKELIST
		(LIKE_ID, USER_EMAIL, PLACE_ID, PLACE_NAME)
		VALUES (LIKELIST_SEQ.NEXTVAL, #{userEmail}, 
		#{placeId}, #{placeName})
	</insert>
	
	<select id="selectList" resultMap="likeResultMap">
		SELECT * FROM LIKELIST 
		WHERE USER_EMAIL = #{userEmail}
		ORDER BY LIKE_ID DESC
	</select>
	
	<select id="selectOne" resultMap="likeResultMap">
		SELECT * FROM LIKELIST
		WHERE USER_EMAIL = #{userEmail} AND PLACE_ID = #{placeId}
	</select>
	
	<delete id="delete">
		DELETE LIKELIST
		WHERE USER_EMAIL = #{userEmail} AND PLACE_ID = #{placeId}
	</delete>
	
</mapper>